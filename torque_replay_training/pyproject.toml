[project]
name = "torque-replay-training"
version = "0.1.0"
description = "Independent full-body torque replay and left prosthesis residual-policy training"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py311"

